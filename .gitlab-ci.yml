stages:
  - build

build:
  stage: build
  image: node:18
  before_script:
    - npm install -g pnpm
  script:
    - pnpm install # install dependencies
    - pnpm build
  cache:
    key:
      files:
        - pnpm-lock.yaml
    paths:
      - .pnpm-store